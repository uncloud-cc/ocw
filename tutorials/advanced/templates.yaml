name: Template Demo
sequence:
  - name: Show Templates
    image: alpine:latest
    cmd: |
      echo "Workflow: {{ workflow.name }}"
      echo "User: {{ env.USER }}"
      echo "Home: {{ env.HOME }}"