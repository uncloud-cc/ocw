name: Build context
sequence:
  - name: Build the container
    id: build
    build:
      image: ocw-tutorials/context
      dockerfile: Dockerfile
      context: ./context

  - name: Run with greeting
    id: run
    image: "{{ steps.build.image }}"
    env:
      NAME: "Mr. 🤖"