name: Template Demo
sequence:
  - name: Demonstrate templates
    image: alpine:latest
    cmd: |
      echo "Workflow: {{ meta.name }}"
      echo "User: {{ env.USER }}"
      echo "Home: {{ env.HOME }}"