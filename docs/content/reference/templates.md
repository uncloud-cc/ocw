# Templates
Templates allow for building reusable and flexible workflows.

**TODO: Continue documenting, once I have a first stable version of the goddamn code!**

## Supported namespaces
`{{ meta.<keyname> }}` - Workflow metadata:
- `{{ meta.runID }}` - Run ID of the workflow execution (unique on every run)

`{{ env.<keyname> }}` - Use environment variables\
`{{ inputs.< }}

## Supported fields
Not all fields support templates - please see a list of supported fields below.

### Switch step
### Run step
### Build step