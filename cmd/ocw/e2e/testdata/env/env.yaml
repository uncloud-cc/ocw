name: Environment Variables Test

env:
  DB_PORT: "5432"
  DB_USER: testuser

sequence:
  - name: Show Env
    image: alpine:latest
    cmd: |
      echo "Port: {{ env.DB_PORT }}"
      echo "User: {{ env.DB_USER }}"
