name: Background Service Test

sequence:
  - name: Start Redis
    id: redis
    image: redis:7-alpine
    background: true

  - name: Use Redis
    image: redis:7-alpine
    cmd: redis-cli -h redis PING
